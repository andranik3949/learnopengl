#version 330 core
layout (location = 0) in vec3 aPos;
layout (location = 1) in vec3 aCol;

uniform float offset;

out vec4 vertexColor;

void main()
{
   vertexColor = vec4(aCol, 1.0f);
   gl_Position = vec4(aPos + vec3(offset, 0.0f, 0.0f), 1.0f);
}