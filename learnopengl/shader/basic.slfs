#version 330 core
in vec2 texCoord;

out vec4 fragColor;

uniform vec3 color;

uniform sampler2D ourTexture1;
uniform sampler2D ourTexture2;

void main()
{
   fragColor = vec4(color, 0.0f);
   //mix( texture(ourTexture1, texCoord), texture(ourTexture2, texCoord), gamma );
}